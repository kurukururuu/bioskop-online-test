// TODO if have time
const handleInvalidForm = (event) => {
  // const {
  //     patternMismatch,
  //     tooLong,
  //     tooShort,
  //     rangeOverflow,
  //     rangeUnderflow,
  //     typeMismatch,
  //     valid,
  //     valueMissing
  // } = event.target.validity;

  // if (patternMismatch) 
  //     event.target.setCustomValidity('...');
  // else if (tooLong)
  //     event.target.setCustomValidity('...');
  // else if (tooShort)
  //     event.target.setCustomValidity('...');
  // else if (rangeOverflow)
  //     event.target.setCustomValidity('...');
  // else if (rangeUnderflow)
  //     event.target.setCustomValidity('...');
  // else if (typeMismatch)
      // event.target.setCustomValidity('...');
  // else if (valid)
  //     event.target.setCustomValidity('...');
  // else if (valueMissing)
  //     event.target.setCustomValidity('...');
}

export default handleInvalidForm